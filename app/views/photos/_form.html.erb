
<%= simple_form_for(@photo) do |f| %>
  <%= f.error_notification %>
  <%= f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present? %>

  <div class="form-inputs">
    <%= f.input :title %>
    <%= f.input :body %>
    <div>
    <%= f.input :photo %>

    <%# if photo.present? %>
      <%# <div id="<%= dom_id(photo, :photo) %>
        <%#= image_tag photo.photo, style: "width: 200px" %>
        <%#= link_to "Remove Image", photo_path(photo), data: { turbo_method: :delete, turbo_confirm: "Sure you want to delete?"} %>
      <%# </div> %>
    <%# end %>

  </div>
  <div>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
